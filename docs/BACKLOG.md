# Backlog priorizado — UnlockHub

> Movido desde CLAUDE.md. Actualizar al final de cada sesión marcando ítems completados con ✅.

## Backlog priorizado

> Actualizar al final de cada sesión marcando ítems completados con ✅.

### 🔴 Bloqueantes — requieren acción del desarrollador

| # | Tarea | Detalle |
|---|---|---|
| P1 | ✅ Migración Prisma en prod | Automática en cada deploy — `npx prisma migrate deploy` en `startCommand` de `railway.json` |
| ~~P2~~ | ✅ Variables Railway configuradas | `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `APP_SCHEME=unlockhub`, `CLOUDINARY_URL`, `ADMIN_SECRET`, `LOGTAIL_SOURCE_TOKEN`, `POSTHOG_API_KEY` — todas en Railway. ✅ Completado |
| ~~P3~~ | ✅ Resend — cuenta + dominio + API key | Configurado — `RESEND_API_KEY` y `RESEND_FROM_EMAIL` en Railway |
| ~~P4~~ | ✅ UMP SDK AdMob | Código integrado — `useGdprConsent.ts` activo, GDPR message ya publicado en AdMob. |
| ~~P4b~~ | ✅ EAS secrets AdMob configurados | Los 4 IDs de producción están en EAS secrets — `HOME_BANNER_ID`, `SEARCH_BANNER_ID`, `INTERSTITIAL_ID`, `REWARDED_ID`. |
| P5 | ✅ Privacy Policy + ToS en URL pública | `docs/privacy-policy.html` + `docs/terms-of-service.html` — GitHub Pages activo, URLs en vivo, datos del desarrollador rellenados. |
| ~~P6~~ | ✅ Google Play Console | Cuenta creada (B7 ✅) — pendiente listing completo antes del submit |
| P7 | ✅ Smoke tests producción — APK #3 completo | APK debug local (build 2026-05-21, 165.7 MB). BUG-3/4/5 re-confirmados ✅. AdMob banners Home+Search ✅. Registro+onboarding ✅. Game detail+Wrapped+perfil público ✅. **BUG-6**: PSN screen muestra flujo NPSSO antiguo (Metro cache stale) — fix: rebuild con `--clean`. Pendiente: vinculación plataformas reales, sync progresivo E2E, Forgot Password (requiere RESEND_API_KEY). |
| B18 | ~~Cuenta RevenueCat + productos + webhook~~ | 🚩 **Diferido a Fase 4** — `FEATURES.premium = false`. El backend está intacto; activar cambiando el flag cuando RC esté configurado. |
| B19 | ~~`EXPO_PUBLIC_REVENUECAT_API_KEY` como EAS secret~~ | 🚩 **Diferido a Fase 4** — no necesario hasta activar `FEATURES.premium = true`. |
| B20 | ~~`REVENUECAT_WEBHOOK_SECRET` en Railway~~ | 🚩 **Diferido a Fase 4** — no necesario hasta activar `FEATURES.premium = true`. |
| T17 | ✅ Verificar que migración gdpr_soft_delete se aplica en Railway | ✅ Verificado en Railway Deploy Logs — 8 migrations found, todas aplicadas incluyendo gdpr_soft_delete |

### 🟡 UX — todas implementadas ✅

| # | Tarea | Estado |
|---|---|---|
| U1 | Ayuda contextual vinculación | ✅ |
| U2 | Centro notificaciones in-app | ✅ |
| U3 | Escudo de racha | ✅ |
| U4 | Filtros en `game/[id].tsx` | ✅ |
| U5 | Wrapped mensual | ✅ |
| U6 | Feedback rate limit 429 | ✅ |
| U7 | Error state en feed | ✅ |
| U8 | Badge solicitudes pendientes | ✅ |
| U9 | Timestamp "última actualización" | ✅ |
| U10 | Sync progresivo: banner + toast en Biblioteca | ✅ |

### 🔵 Técnica

| # | Tarea | Estado |
|---|---|---|
| T1 | Paginación biblioteca | ✅ |
| T2 | Reconexión Socket.io | ✅ |
| T3 | Background sync scheduler | ✅ |
| T4 | ✅ Paginación cursor en feed | ✅ Cursor-based: `getFriendsFeed(userId, limit, cursor?)` devuelve `CursorPaginatedResponse<ActivityEvent>` con `nextCursor`. `useFeed` usa `useInfiniteQuery` con `pageParam` como cursor. |
| T5 | Tests de carga k6 | ✅ |
| T6 | Tests unitarios nuevos servicios | ✅ |
| T7 | Reescribir FeedScreen.test.tsx | ✅ Reescrito correctamente — mockea `useMyGames`; 9 tests pasando |
| T8 | Subir Expo a v55 para vulnerabilidades node-tar | ✅ Completado sesión 56 — Expo 55.0.26, RN 0.83.6, React 19.2.0, reanimated 4.2.1+worklets 0.7.4, flashlist 2.0.2, rn-google-mobile-ads 16.3.3, sentry 7.11.0. 352/352 tests. 0 errores TS/lint. |
| T9 | Resolver 145 warnings import/order en API | ✅ Resuelto — `eslint --fix` + override en `.eslintrc.js` para ficheros de test |
| T10 | Flows Maestro E2E | ✅ 5 flows en `apps/mobile/.maestro/` — todos pasando contra emulador Android con APK preview |
| T11 | Search de logros + endpoint logros de juego | ✅ Backend `GET /api/v1/games/:id/achievements` + `GET /api/v1/search?type=achievements` — JWT opcional, Xbox excluido, paginado 20/pág |
| T12 | Job "seed de logros populares" | ✅ Completo — BD producción (post-limpieza PL13, sesión 59): **2.878 juegos + 134.928 logros**. Seed inicial: 1.406 juegos (78 Steam + 1.001 RA + 327 PSN) + 72.264 logros. Bugs PSN corregidos: guard `trophies ?? []` + refresco token cada 5 usuarios. Campo `console` backfilled: RA (1.001 juegos) + PSN (584 juegos). Flag `--only-steam --usernames="X"` implementado en `scripts/seed-games.ts`. **Seithek Steam**: 0 juegos obtenidos — perfil de Steam privado (`GetOwnedGames` devuelve lista vacía). Hacer público en Configuración de Steam para seedear. |
| T13 | Sync optimization: skip completed RA games | 🔲 El parallel RA batches (F15 ✅) ya está implementado con `RA_PROCESS_CONCURRENCY=3` + `Promise.allSettled`. Lo que queda es "skip completed": saltar juegos donde el usuario ya tiene todos los logros para evitar llamadas innecesarias a la API. Diferido: riesgo de perder logros de DLC añadidos post-sync. Documentado en DECISIONS.md. |
| T14 | Desnormalizar contadores de biblioteca (`earnedAchievements`, `totalGames`) | 🔲 Diferido. A la escala actual (<10k UserAchievements por usuario típico) el cálculo en JS sobre la query indexada por `userId` es eficiente. Solo aporta valor real para power users con >100k achievements (PSN con 2000+ juegos × 50 trofeos) — caso hipotético con el catálogo actual (134.928 logros totales en 2.878 juegos). Requiere migración Prisma + lógica de actualización en los 3 adapters. Ver DECISIONS.md. |
| T15 | Steam skip-completed optimization via `rtime_last_played` | 🔲 `GetOwnedGames` devuelve `rtime_last_played` (Unix timestamp) por juego. Usarlo para saltar juegos sin actividad reciente reduciría llamadas a Steam. No implementado: requiere añadir `lastPlayedAt` al modelo de caché Redis + interfaz `GameCacheEntry`, con riesgo de saltar achievements de DLC. Documentar como pendiente post-lanzamiento. |
| T16 | ✅ Backfill RA XP con fórmula correcta | Script `scripts/backfill-ra-xp.ts` creado e idempotente — ejecutado en producción. 48.865 logros RA procesados — 29.419 actualizados (fórmula vieja `min(100, max(1, points))` → correcta `max(5, round(points/5))`), 28.583 saltados (ya correctos), 0 errores. XP de usuarios se corregirá gradualmente en próximo sync automático. |
| T17 | BUG-CRÍTICO-1/2 + BUG-MEDIO-3/4 corregidos | ✅ Sesión 31: soft delete GDPR, email eliminado de perfil público, RA syncUserExpress con Promise.allSettled, deletedAt:null filters, gdpr-cleanup scheduler, authenticate con DB check, rankings RefreshControl fix, upsertUserScore paralelo, useSubscription RC CustomerInfo |
| T18 | ✅ Migración Prisma gdpr_soft_delete en producción | ✅ Verificado en Railway Deploy Logs — 8 migrations found, todas aplicadas incluyendo gdpr_soft_delete |
| T19 | BUG-1: biblioteca no carga sort completo al abrir | ✅ useEffect([isLoading]) en index.tsx — sesión 34 |
| T20 | BUG-2: pull-to-refresh pierde páginas con sort activo | ✅ handleRefresh llama fetchAllRemainingPages cuando sort activo — sesión 34 |
| T21 | BUG-3: rankings PSN XP desincronizado entre "mi posición" y lista | ✅ getUserRank lee sorted set correcto por plataforma — sesión 34 |
| T22 | BUG-4: vinculación plataformas no bloqueaba perfil privado | ✅ PSN/RA/Steam devuelven 400 con código descriptivo antes de vincular — sesión 34 |
| T23 | BUG-1: FriendshipButton no aparecía en perfil público | ✅ Fallback a "Añadir amigo" en error de red; spinner durante hidratación de sesión — sesión 36 |
| T24 | BUG-2: búsqueda mostraba propio usuario + redirect incorrecto al propio perfil | ✅ searchUsers excluye userId autenticado; profile/[username] redirige a tab Profile si username coincide — sesión 36 |
| T25 | BUG-3: biblioteca no cargaba páginas completas al abrir con caché | ✅ initialLoadDoneRef con deps completas — cubre caso con y sin caché — sesión 36 |
| T26 | BUG-4: rankings plataforma mostraban XP total en "Mi posición" | ✅ Fix completo sesión 38 — 4 puntos rotos: `getUserRank` sin platform, controller sin `?platform`, `useMyRanking` sin platform, `rankings.tsx` sin argumento. `ranking.controller.test.ts` (8 tests) + 3 tests RankingsScreen banner PSN/RA |
| T27 | ✅ Fetch achievements on-demand al pulsar juego con 0 logros en Search | ✅ `POST /api/v1/games/:id/fetch-achievements` — `fetchAndUpsertGameAchievements` en `games.service.ts`; guard 24h si `totalAchievements > 0`; Steam: `GetSchemaForGame` + `GetGlobalAchievementPercentagesForApp`; RA: `API_GetGameInfoAndUserProgress.php` con credenciales del sistema; PSN: no-op (PSN siempre llega con logros desde sync). Mobile: botón "Cargar logros" en `game/[id].tsx` cuando `totalAchievements === 0` y usuario autenticado; loading state; invalidación de query post-fetch. |
| T28 | FIX-1: biblioteca carga páginas completas al volver del background | ✅ `initialLoadDoneRef.current = false` en mount + AppState handler — sesión 37 |
| T29 | FIX-2: deduplicación juegos en useMyGames | ✅ `Set<string>` filtra solapamientos entre páginas — sesión 37 |
| T30 | FIX-3: optimistic update FriendshipButton | ✅ `onMutate` en `sendRequest`/`cancelOrRemove`/`accept` — cambio instantáneo con rollback en error — sesión 37 |
| T31 | FIX-4: empty state "Tus juegos aparecerán pronto" incorrecto tras desvincular | ✅ `unlinkMutation.onSuccess` invalida `['sync-summary']` — sesión 37 |
| T32 | FIX-5: búsqueda de logros eliminada del Search | ✅ `search.tsx` solo busca juegos y usuarios — hook y endpoint backend intactos para T27 — sesión 37 |
| T33 | FIX-6: contador "0 logros" ocultado en GameCard | ✅ Omitido cuando `totalAchievements === 0` — sesión 37 |
| T34 | Rankings: banner "Tu posición" mostraba XP total en filtros de plataforma | ✅ Triple fix: getUserRank lee sorted set correcto, queryKey incluye platform, URL incluye ?platform — sesión 38 |
| T35 | BUG-1: lista no ordenada al entrar ni en pull-to-refresh para sort last_played | ✅ Eliminado early return y condición en useEffect y handleRefresh — todos los sorts cargan todas las páginas — sesión 39 |
| T36 | BUG-2: library.new_games_banner aparecía como texto literal | ✅ Selección explícita de clave _one/_other en NewGamesBanner.tsx — sesión 39 |
| T37 | Steam sync falla con rawValue/rarity string en lugar de Float | ✅ `parseFloat(String(rawRarity))` + guard `isNaN` en ambos loops de logros · guard `startsWith('http')` en `iconUrl` — sesión 40 |
| T38 | Deploy Railway fallaba: gdpr-cleanup.scheduler.ts usaba conexión Redis incompatible con BullMQ Worker | ✅ `createWorkerConnection()` en lugar de `redis` directo — sesión 41 |
| T39 | Bug: juegos de plataforma desvinculada seguían en biblioteca | ✅ unlinkPlatform usaba relation filter no fiable en Prisma — fix con IDs explícitos del findMany previo — sesión 43 |
| T40 | Eliminar NewGamesBanner — intrusivo e inexacto en pull-to-refresh | ✅ Componente, tests, claves i18n y lógica eliminados completamente — sesión 43 |
| T41 | Sync secuencial por usuario — evita "No se pudo cargar la biblioteca" durante syncs simultáneos | ✅ Lock Redis `sync:user-lock:{userId}` con reencolado · concurrencia global intacta · sesión 44 |
| T42 | Empty state "Tus juegos aparecerán pronto" incorrecto al desvincular con varias plataformas | ✅ `refetchQueries` forzado + skeleton durante `isFetching` — sesión 44 |
| T43 | Lock de sync no cubría `triggerExpressSync` — Steam+PSN express simultáneos al onboarding | ✅ `triggerExpressSync` adquiere `sync:user-lock:{userId}` con TTL 120s; omite si lock tomado — sesión 45 |
| T44 | ✅ **CODE SMELL**: Duplicación `uploadAvatar`/`uploadBanner` en `user.controller.ts` | `makeUploadHandler(serviceMethod)` factory — `uploadAvatarHandler` y `uploadBannerHandler` son ahora `const` derivados. Sesión 63 |
| T45 | ✅ **CODE SMELL**: Duplicación `uploadAvatar`/`uploadBanner` en `upload.middleware.ts` | `createUploadMiddleware(field)` factory — las dos exportaciones son `createUploadMiddleware('avatar')` y `createUploadMiddleware('banner')`. Sesión 63 |
| T46 | ✅ **CODE SMELL**: QueryKeys dispersas en múltiples hooks (no centralizadas) | `lib/queryKeys.ts` con 30+ claves tipadas. Reemplazados todos los usos en 15 hooks + 12 screens. Fix colateral: `profile.tsx` invalidaba `['my-stats']` (no-op) — corregido a `queryKeys.userStats()`. Sesión 63 |
| T47 | ✅ **CODE SMELL**: Debounce duplicado en `useSearch.ts` y `useSearchAchievements.ts` | `hooks/useDebounce.ts` con `useDebounce<T>(value, delay)` — elimina `timerRef` y `useEffect` en ambos hooks. Sesión 63 |
| T48 | ✅ **CODE SMELL**: IDs de test de AdMob hardcodeados en 3 ficheros | `lib/adUnits.ts` con `ADMOB_TEST_IDS` (BANNER, INTERSTITIAL, REWARDED). Sesión 63 |
| T49 | ✅ **CORREGIDO**: `background-sync.scheduler.ts` condición `gte` → `lte` en `lastSyncAt` | Bug confirmado: `gte: oneDayAgo` sincronizaba usuarios que SÍ habían sincronizado recientemente (opuesto al objetivo). Corregido a `lte: oneDayAgo` — ahora sincroniza usuarios que llevan más de 24h sin sincronizar. Sesión 56 |
| T50 | ✅ **COBERTURA TESTS**: Auth — tests de refresh token de usuario con soft delete | Añadidos en `auth.routes.test.ts`: (1) `POST /refresh` 401 cuando tokens revocados por `deleteAccount`; (2) `GET /me` 401 ACCOUNT_DELETED cuando `findUnique` con `deletedAt:null` devuelve null. Mock de `prisma.user.findUnique` añadido al fichero. Sesión 56 |
| T51 | ✅ **COBERTURA TESTS**: Points — test de race condition en `claimRewardedAdPoints` | Añadido en `points.service.test.ts`: dos llamadas simultáneas con `Promise.allSettled`, solo la primera adquiere lock (`SET NX` devuelve `'OK'`), la segunda recibe `REWARDED_AD_COOLDOWN` 429. `mockCreate` llamado exactamente 1 vez → saldo +10 no +20. También corregidos los tests previos para reflejar la firma `SET NX EX` de 5 args. Sesión 56 |
| T52 | ✅ Cachear datos de juegos para acelerar carga de biblioteca | Sesión 54 — `game-cache.ts`: clave `game:meta:{platform}:{externalId}` TTL 24h; adapters PSN/RA/Steam comprueban caché antes de cada `game.upsert`; syncs repetidos no generan escrituras a PostgreSQL para juegos ya conocidos. |
| T53 | ✅ Fix crash por sync largo | Sesión 54 — 4 fixes: `syncProgressKey` en `finally` de `sync.worker.ts`; guard `MAX_PAGES=10` en `fetchUserTitles` de `psn.adapter.ts`; claves stale RA con TTL 7 días en `retroachievements.adapter.ts`; throttle 15s en handler socket de `useSyncProgress.ts`. |
| T54 | ✅ Refactor general post-lanzamiento (T44–T48) | ✅ Sesión 63 — BUILD_LOCAL.md duplicado eliminado. `createUploadMiddleware()` factory (T44/T45). `lib/queryKeys.ts` con 30+ claves centralizadas (T46). `hooks/useDebounce.ts` (T47). `lib/adUnits.ts` `ADMOB_TEST_IDS` (T48). Fix bug colateral: `['my-stats']` → `['user-stats']` en `profile.tsx`. 593/593 API · 364/364 mobile. 0 errores TS/lint. |
| T55 | ✅ Fix edge-to-edge Android 15 — contenido desplazado hacia arriba | Sesión 54 — todos los tabs cambiados a `edges={['left', 'right']}` en `SafeAreaView`; el header de React Navigation gestiona el inset superior y el tab bar el inferior. Sin el fix, `targetSdkVersion=35` contaba el safe area inset del status bar dos veces. |
| T56 | ✅ Fixes de seguridad sesión 53 | ✅ En develop — `xbox.adapter.ts`: doble cifrado AES-256-GCM eliminado · `search.service.ts`: filtro `deletedAt: null` añadido en `searchUsers` · `user.service.ts`: revocación `RefreshToken`s añadida a transacción de borrado de cuenta |
| T57 | ✅ Modo claro UI | ✅ Implementado en rama `feat/t57-light-mode` — `lib/colors.ts` (darkColors/lightColors), `hooks/useTheme.ts`, `preferencesStore.theme: 'dark'|'light'`, selector de tema en profile.tsx (🌙/☀️ activo). 22 ficheros actualizados: 9 componentes globales + 5 tabs + 3 pantallas. Inline styles reemplazan clases NativeWind hardcoded. 364/364 tests ✅, 0 errores TS/lint. |
| T58 | ✅ Fix `platformAccount.update` → `upsert` (race condition P2025) | Sesión 69 — 6 ocurrencias en `retroachievements.adapter.ts`, `sync.service.ts`, `xbox.adapter.ts` y `sync.worker.ts`. Durante syncs concurrentes del mismo usuario, un `update` podía recibir error P2025 "Record to update not found" si otro job había borrado/recreado el registro en paralelo. El `upsert` es idempotente y resuelve la race condition. |
| T59 | ✅ V3 — Workers BullMQ separados a `apps/worker` (proceso Railway dedicado) | Sesión 69 — nuevo `apps/worker/src/index.ts` arranca sync, streak, challenge, gdpr-cleanup, seed-catalog workers + schedulers con cierre limpio `SIGTERM`/`SIGINT`. `apps/api/src/index.ts` limpiado de workers. Servicio `unlockhub-worker` en Railway con 14 Shared Variables. Fallback: `getIOSafe()` desde worker devuelve null — Socket.io usa polling Redis. Para eventos en tiempo real: añadir `@socket.io/redis-emitter`. |
| T60 | ✅ Fix worker Railway — Dockerfile propio + Config File Path | Sesión 70 — `apps/worker/Dockerfile` (build multi-stage, tsx runtime, WORKDIR /app). `apps/worker/railway.json` como Config File Path en Railway dashboard. `railway.json` raíz: `preDeployCommand` simplificado a `npx prisma migrate deploy` (sin `cd apps/api` — el Dockerfile raíz ya tiene `WORKDIR /app/apps/api`). package-lock.json regenerado con @unlockhub/worker@0.0.1. API: 10 migrations found, arrancada port 8080. Worker: todos los schedulers BullMQ activos, syncs procesándose. |
| T61 | ✅ BUG-A fix unlinkPlatform — invalidar caché Redis pública tras desvincular | Sesión 70b — `invalidateUserPublicCache(userId)` añadido en `platform.service.ts` después de cancelAutoSync. Sin el fix, la caché `user-games:*` permanecía 5 min con juegos de la plataforma desvinculada. Test añadido en `platform.service.test.ts`. |
| T62 | ✅ BUG-B fix edge-to-edge `app/user-game/[username]/[gameId].tsx` | Sesión 70b — `edges={['top','left','right']}` (antes solo `['left','right']`). Root layout tiene `headerShown: false`, sin header de React Navigation el contenido subía bajo el status bar. |
| T63 | ✅ BUG-C fix edge-to-edge `app/(tabs)/profile.tsx` | Sesión 70b — `edges={['left','right']}` añadido al SafeAreaView principal (línea 358). Sin `edges`, el default incluye `top` duplicando el inset ya gestionado por el header del Tabs navigator. |
| T64 | ✅ BUG-D fix orden biblioteca usuario público — lastActivityAt DESC | Sesión 70b — `getMyGames` ordena por `lastActivityAt DESC` (null last) en lugar de alfabético. La biblioteca propia re-ordena en cliente; la pública mostraba juegos en orden incorrecto. Test actualizado. |
| T65 | ✅ Auditoría calidad apps/mobile — 17 fixes (socket leaks, i18n duplicados, param guards, AsyncStorage, queryKeys, SafeAreaView, AdMob events) | Sesión 70c — commit dea6cbd |
| T69 | ✅ Segunda auditoría completa apps/mobile — 17 fixes AdMob lifecycle, i18n PremiumBanner, queryKeys rankings, socket guards, AsyncStorage timing | Sesión 71 — commit 0503d55 |
| T70 | ✅ Fix uploadFile — XMLHttpRequest para multipart en React Native (avatar y banner) | Sesión 72 — commit uploadFile |
| T71 | ✅ Fix ruta rewarded-ad — '/api/v1/points/rewarded-ad' → '/api/v1/users/me/points/rewarded-ad' | Sesión 72 — commit 7023e7d |
| T72 | ✅ Fix bannerMutation — actualiza store sesión en tiempo real tras upload | Sesión 72 |
| T73 | ✅ Fix loginHandler/meHandler — devuelven perfil completo con avatar, banner y todos los campos | Sesión 72 — commit 01e00f9 |
| T74 | ✅ Fix banner no se actualizaba en tiempo real tras subida — bannerMutation.onSuccess ahora actualiza el store Zustand + invalida queryKeys.me(), simétrico a avatarMutation | Sesión 73 |
| T75 | ✅ CVEs ws + tar — `npm audit fix` (A1/A8) + A2 riesgo aceptado | ✅ S1/S6c — ws runtime 8.x→8.20.1 (engine.io 6.6.7→6.6.8, socket.io-adapter 2.5.6→2.5.7); express 4.22.1→4.22.2, qs y brace-expansion bumpeados. tar (A2): override (`"tar": "^7.5.11"`) y override anidado (`@mapbox/node-pre-gyp > tar`) probados en npm 11.12.1 — no aplicables: npm no cruza major-version boundaries en overrides de dependencias de workspaces. `npm audit fix --force` descartado. **Riesgo aceptado S6c**: tar solo se ejecuta en `npm install`; superficie de ataque requiere comprometer npm registry bajo HTTPS. Alternativa futura: `bcrypt@5→6` (elimina @mapbox/node-pre-gyp y tar) — diferida a auditoría post-lanzamiento. |
| T76 | ✅ `webhooks.controller.ts:46` — `token !== secret` → `crypto.timingSafeEqual()` (A3) | ✅ S1 — `Buffer.from(sha256(a))` vs `Buffer.from(sha256(b))` con `crypto.timingSafeEqual()`. Tests: prefijo-corto y sufijo-largo añadidos. |
| T77 | ✅ `getMyRankHandler` — validar `platform` query con `platformSchema.parse()` (A6) | ✅ S1 — `platformSchema.parse(req.query['platform'])` + test plataforma inválida → 400. |
| T78 | ✅ Upload middleware — validación magic bytes además de MIME type (A5) | ✅ S1 — `validateFileMagicBytes` con firmas manuales JPEG `FF D8 FF`, PNG `89 50 4E 47 0D 0A 1A 0A`, WebP `RIFF/WEBP`. Sin deps externas nuevas. 6 tests añadidos. |
| T79 | ✅ `no-floating-promises` / `no-misused-promises` en ESLint (A7) | ✅ S1 — `apps/api/.eslintrc.js` + `tsconfig.eslint.json` (incluye tests). Fix real encontrado: `io.close()` (socket.io 4.8.3 devuelve `Promise<void>`) → `await io.close()` en SIGTERM handler. |
| T80 | ✅ Eliminar 7 `console.log` debug de producción en móvil (A10) | ✅ S5 — completado. 0 `console.log/error` en `apps/mobile` en producción. |
| T81 | ✅ Silenciar falso positivo `security/detect-unsafe-regex` en `useWrapped.ts:15` (A9) | ✅ S6b — `// eslint-disable-next-line security/detect-unsafe-regex` añadido con comentario justificativo. |
| T82 | `authenticate` middleware: documentar el trade-off del catch bypass (A4) | 🔲 S1 — Añadir comentario explícito sobre el riesgo aceptado y logging estructurado del evento para trazabilidad. |
| T83 | ✅ Auditoría S1 — Seguridad backend completa | ✅ A1/A3/A5/A6/A7/A8/A14-A19 resueltos. A20 descartado (mitigado por authRateLimiter). A4/A21 ⚙️ pendientes de verificación. A2 diferido a S6. Ver `docs/AUDIT.md` para detalle completo. Tests: 620 API (+9) · 387 mobile · 0 TS/lint. |
| T84 | ✅ Auditoría S2 — Sync e integraciones externas | ✅ A4 verificado: token corrupto/expirado siempre → 401 (catch aislado a errores de BD). A21 verificado: cooldown Redis SET NX es la primera op de `triggerManualSync`. A23 ✅: `timeout: 10_000` añadido a 6 fetchers de Steam (4 en `getUserAchievements` + 2 en `fetchSteamAchievementDefinitions`). A24 ✅: **bug crítico** — contador `steam:api:calls:<date>` nunca se incrementaba (protección del 80% estaba inactiva); `incrementSteamApiCounter()` con `redis.incr` + TTL 48h añadido en todos los fetchers reales (cache misses). A25 ✅: `timeout: 15_000`/`10_000` + `XBOX_MAX_PAGES=20` en `xbox.adapter.ts`. A26 ✅: `?? []` guards en `earnedRes.trophies` y `titleTrophiesRes.trophies` en `psn.adapter.ts`. A27 🔲 diferido a S6: timeout en llamadas psn-api (requiere wrapping de librería externa). Tests: 620 API · 387 mobile · 0 TS/lint. |
| T85 | ✅ Auditoría S3 — Rendimiento backend y capa de datos | ✅ A28: `upsertUserScore` 4 queries → 1 (`getPlatformXpMap`). A29/A32: `select` explícito en `getUserGameAchievements`, `getProfile` y `getPublicProfile` — cierra fuga PII (`passwordHash`/`birthDate`/`role`/`deletedAt` fuera del fetch). A30: TTL deslizante 20 min en `USER_GAMES_KEYS_SET`. A31: `invalidateUserPublicCache` en `deleteAccount` — limpieza GDPR completa en Redis. **A33–A36 ✅ aplicados en producción (2026-06-12)**: 5 índices compuestos creados con `CREATE INDEX CONCURRENTLY` manualmente fuera de transacción vía `prisma db execute --file`, `indisvalid=true` verificado en todos, cada migración marcada con `migrate resolve --applied`. Método obligatorio para futuros índices sobre tablas grandes — ver convención en CLAUDE.md y INC-01 en AUDIT.md. A37–A40 diferidos a S6. A41 identificado (discrepancia umbrales Steam 80/90% en docs vs código). Tests: 620 API · 387 mobile · 0 TS/lint. |
| T86 | ✅ Auditoría S4 — Mobile, memory leaks y fluidez | ✅ A42: `useSyncProgress` eliminado de `SyncStatusBar` — single source of truth en `LibraryScreen` (prop `isRunning: boolean`); elimina 2 sets de listeners Socket.io (`sync:progress`, `sync:complete`, `sync:error`) + 2 timers de gracia + 2 intervals de polling fallback que se duplicaban durante cada sync. A43: selectores Zustand precisos en 5 hooks (`useInterstitialAd`, `useRewardedAd`, `useCompletedGamesInterstitial`, `useWrappedInterstitial`, `useMyGames`) — sin selector, cada cambio de XP/nivel tras sync re-renderizaba los hooks de AdMob. A10: 4 `console.log` eliminados de `useRewardedAd.ts` (fuera del critical path del rewarded ad — adelantado de S6); 3 restantes en `profile.tsx:228` y `api.ts:180` diferidos a S6. A44/A45 reasignados a S5. Tests: 387 mobile · 620 API · 0 TS/lint. |
| T87 | ✅ Auditoría S5 — Mobile, seguridad y datos | ✅ A44: `lib/cloudinary.ts` — `getCloudinaryThumb(url, w, h)` aplicado en 5 puntos (UserCard, RankingItem, ActivityCard, profile.tsx, profile/[username].tsx) — imágenes servidas al tamaño exacto de render; 6 tests. A45: `useSyncStatus` refetchInterval dinámico — apaga polling cuando `isRunning=false`, activa a 2 s durante sync activo; 2 tests. A46: `Sentry.init` `beforeSend` en `_layout.tsx` — redacta `Authorization` header y body en rutas `/auth/*`; verificación pendiente de cobertura url/query/breadcrumbs. A48: `refreshAccessToken()` detecta 401 → `deleteRefreshToken()` + `clearSession()` — sesión inválida ya no queda viva sin redirigir al login. A10 completado: 0 `console.log/error` en `apps/mobile` en producción. A49: UMP consent vs AdBanner — diferido a S6a PRE-LANZAMIENTO (cerrar antes de promover a Producción). Tests: 395 mobile (+8) · 620 API · 0 TS/lint. |
| T88 | ✅ Auditoría S6a — A49 UMP consent vs AdBanner (GDPR/Play Store) | ✅ `consentResolved: boolean` (inicial `false`) añadido a `preferencesStore` — no se persiste en AsyncStorage (se resuelve en cada arranque). `useGdprConsent` llama `setConsentResolved(true)` en bloque `finally` (cubre NOT_REQUIRED, REQUIRED y errores) y en early return cuando el módulo nativo no está disponible. `AdBanner` gatea su render con `if (!consentResolved) return null`. 5 tests nuevos: 2 en `AdBanner.test.tsx` + 3 en `__tests__/hooks/useGdprConsent.test.ts`. Tests: 400 mobile (+5) · 620 API · 0 TS/lint. |
| T89 | ✅ Auditoría S6b — A41 cuota Steam 90 % (manual sync) | ✅ `apps/api/src/config/steamQuota.ts` centraliza `STEAM_DAILY_LIMIT`, `STEAM_BACKGROUND_SYNC_THRESHOLD` (0.8) y `STEAM_MANUAL_SYNC_THRESHOLD` (0.9). `background-sync.scheduler.ts` migrado a importar del módulo compartido. `triggerManualSync` comprueba contador Redis antes de encolar: si ≥ 90 % y usuario tiene otras plataformas → libera cooldown + `skippedByQuota: true`; si Steam es la única plataforma → `STEAM_QUOTA_EXCEEDED` 429. Controller: HTTP 200 para `skippedByQuota`, 202 para sync real. Mobile: `useSyncAll` detecta `STEAM_QUOTA_EXCEEDED` (rejected) y `skippedByQuota: true` (fulfilled) → `steamQuotaState: 'exceeded'\|'skipped'\|null`. `SyncStatusBar` muestra aviso no bloqueante en rojo (exceeded) o ámbar (skipped). 5 tests nuevos: 3 API + 2 mobile. Tests: 623 API (+3) · 402 mobile (+2). |
| T91 | ✅ Auditoría S6d — A37 `getPublicFeed` cursor-based | ✅ `getPublicFeed(limit, cursor?)` devuelve `CursorPaginatedResponse<ActivityEvent>` — sin `count()`, sin `skip`. `feedQuerySchema` en controller (eliminado `paginationSchema`). `hooks/usePublicFeed.ts` + `queryKeys.publicFeed()`. Cutover limpio: no había consumidor mobile activo. 4 tests API + 1 controller actualizado + 5 tests mobile. Tests: 627 API (+4) · 407 mobile (+5) · 0 TS/lint. |
| T92 | ✅ Auditoría S6b — A22, A39, A9, A12, A13 + barrido higiene | ✅ **A22**: `triggerExpressSync` ahora llama `queueInitialSync` como fallback cuando el lock no está disponible — sin doble encolado cuando sí lo adquiere; 2 tests nuevos. **A39**: `loadUserAchievements` en wrapped.service.ts migrado a `select` explícito (5 campos achievement + 4 game — elimina 6 campos no usados); tipo `UserAchievementFull` actualizado. **A9**: eslint-disable-next-line en `useWrapped.ts:15`. **A12**: `apps/api/.depcheckrc` con ignore `pino-pretty`. **A13**: `apps/worker/.depcheckrc` con ignore de 6 deps workspace. Barrido: 0 ciclos (madge 375 archivos) · i18n ES=EN 627 claves · 0 queryKeys literales en producción · `usePublicFeed` huérfano documentado (sin pantalla consumidora). Tests: **629 API (+2) · 407 mobile · 0 TS/lint**. |
| T95 | 🔲 Instrumentación analítica post-lanzamiento (eventos no críticos) | Eventos aplazados a post-lanzamiento: `achievement_viewed`, `premium_paywall_seen`, `premium_purchased`, `points_redeemed`, `friend_challenged`, `guide_written`, `rewarded_ad_claimed`, `profile_shared`, `search_performed`. No instrumentar hasta tener retención D7 estabilizada (riesgo: ruido en dashboard antes de tener volumen). Requiere: (a) confirmar PostHog captura los 5 eventos del lanzamiento (`platform_linked`, `sync_completed`, `wrapped_shared`, `app_open`, `onboarding_completed`); (b) definir dashboard PostHog con D1/D7 retention y `sync_completed` rate antes de añadir más eventos. |
| T93 | 🔲 Implementar pantalla de feed público o eliminar `usePublicFeed` | Hook `hooks/usePublicFeed.ts` + endpoint API `GET /api/v1/activity/public` (cursor-based, A37 ✅) están listos pero ninguna pantalla mobile los consume. Decidir: (a) crear `app/(tabs)/feed.tsx` como pantalla pública de actividad global; (b) eliminar el hook si la feature se descarta. Pendiente de roadmap Fase 4. |
| T90 | 🔲 Paginación por cursor en Steam sync por usuario (deuda de escalabilidad) | Fase 4/post-lanzamiento. Con pocos cientos de usuarios el sync completo de Steam (hasta 3.000+ juegos/usuario) cabe en un job BullMQ. A escala media-alta, un solo usuario con biblioteca grande puede bloquear el pool de workers durante minutos. **Propuesta**: cursor-based sync — cada job procesa N juegos (100–200), persiste el cursor (`lastSyncedGameId`) en Redis (`steam:sync-cursor:{userId}`, TTL 24h) o en `PlatformAccount.syncCursor` (nuevo campo), y reencola el siguiente lote. Prioridad de juegos: ordenar por `rtime_last_played` (Steam API) — los más jugados recientemente primero. El scheduler de background (`background-sync.scheduler.ts`) retoma desde el cursor si existe. **Relación con A41**: complementario, no sustituto — A41 frena cuando el presupuesto diario se acerca al límite; este mecanismo evita que usuarios con 3k juegos consuman el presupuesto por completo en un solo sync. **Relación con A51**: A51 implementa la versión mínima sin cursor (tope fijo de 100 juegos + ordenación por rtime_last_played) como control de costes pre-lanzamiento; T90 añade reanudación con cursor para recuperar los juegos omitidos en syncs sucesivos. Implementar cuando el volumen de usuarios y la duración de syncs en Railway logs indiquen saturación del pool. |
| T96 | ✅ BUG-Wrapped: XP ganados mostraba 0 en Wrapped (todos los períodos) | Rama `fix/wrapped-xp-zero` (commit `04e8a9c`, sesión 2026-06-21). `totalXpGained` calculaba sobre `UserPoint.createdAt` (fecha del sync — siempre 2026) en lugar de la fecha real de los logros. Fix en `wrapped.service.ts`: `achievementXp = sum(ua.achievement.normalizedPoints)` donde `ua.unlockedAt` está en el período; `streakXp = sum(UserPoint.amount, reason: STREAK, createdAt en período)`. `prevStats` se corrige solo. Verificado en device: 42.200 XP en Wrapped 2025 (antes: 0). Solo backend. |
| T94 | ✅ Auditoría S7 — A51 tope de juegos Steam por sync (control de costes pre-lanzamiento) | ✅ `STEAM_MAX_GAMES_PER_SYNC = 100` en `apps/api/src/config/steamQuota.ts` (junto a los umbrales 80/90 % de A41). En `syncUser` y `syncUserBatched`: juegos elegibles ordenados por `rtime_last_played` desc (señal primaria Steam) + `playtime_forever` como desempate; se procesan los primeros 100. Log pino INFO con `{userId, total, syncing, skipped}` cuando hay omisiones. El `total` de progreso refleja solo los juegos procesados (el cliente ve 100 % al terminar). Contador `steam:api:calls:<date>` solo se incrementa para juegos procesados (coherente con A24). `syncUserExpress` no se toca (ya tiene su propio tope de 20). `gamesSkipped` no se propaga al socket ni al mobile (coste alto: requeriría cambios en SyncCompleteEvent, worker y hooks mobile). Deuda de reanudación con cursor: T90 (Fase 4). 3 tests nuevos. Tests: **632 API (+3) · 407 mobile · 0 TS/lint**. |
| T97 | 🔲 Health check de credenciales de plataforma al arranque del worker | Al arrancar `apps/worker/src/index.ts`, verificar que las credenciales de sistema de las 3 plataformas autentican correctamente antes de que los workers empiecen a procesar jobs: RA — llamada ligera a `getUserSummary` con `RA_SYSTEM_USER/KEY`; PSN — intercambio de `PSN_SYSTEM_NPSSO` por access token vía `getSystemPsnAuth()`; Steam — llamada simple a `GetSupportedAPIList` con `STEAM_API_KEY`. Si alguna falla, loggear `{ credential: 'RA_SYSTEM_KEY', httpStatus: 401 }` como `logger.warn` — **sin bloquear el arranque** (el worker puede seguir funcionando para las plataformas que sí autentican). Previene el escenario INC-RA-001 (2026-06-27): credencial incorrecta detectada en logs de sync horas después del deploy, en lugar de detectarse inmediatamente en el arranque. Ver OP08 para el procedimiento operativo complementario. |
| T98 | ✅ **Hardening Dockerfile — non-root user (CWE-250) — VERIFICADO EN PRODUCCIÓN** | `apps/api/Dockerfile` y `apps/worker/Dockerfile` ahora corren el proceso como usuario `node` (uid 1000). Patrón: instalación/compilación (bcrypt@6 nativo, apk add/del) como root → `chown -R node:node /app` → `USER node` antes del CMD. Fix adicional worker (commit `e31fc95`): `ENV XDG_CACHE_HOME=/app/.cache` para que tsx escriba su caché bajo directorio propiedad de `node`. Verificado en Railway (env production, 2026-06-27): API login real exitoso (bcrypt@6 bajo uid 1000 ✅); worker 5 schedulers activos + sync PSN completado sin fallo de permisos ✅. Ver A57 en AUDIT.md. |
| T99 | ✅ Revisión manual crypto.ts (CWE-310) — **FP verificado, cerrado** | Verificación manual completa: AES-256-GCM, IV `randomBytes(12)` por operación (no determinístico, no reutilizado), auth tag GCM generado y validado en decrypt, clave 32 bytes validada con regex. Implementación canónica. Cifra tokens PSN/Xbox en reposo. El scanner usó heurísticas léxicas sobre el nombre de variable, no análisis semántico. Sin acción de código. Ver A62. |
| T100 | ✅ js-yaml 3.14.2 EOL — **FP de producción, cerrado** | Transitiva exclusiva de `devDependencies` (`ts-jest → @jest/transform → babel-plugin-istanbul → @istanbuljs/load-nyc-config → js-yaml@3.14.2`). 0 imports en código propio. No entra en el runner de producción (`npm ci --omit=dev`). Sin riesgo operativo. Ver A63. |
| T101 | ✅ **Bug — Reencolado en bucle de syncs concurrentes del mismo usuario — RESUELTO (opción E, 2026-06-29)** | **Causa raíz resuelta**: los repeat jobs single-platform de `restoreAutoSyncs`/`scheduleAutoSync` competían por el lock de usuario `sync:user-lock:{userId}` → reencolados en bucle. Además el batch de fondo estaba muerto porque faltaba `new Worker('background-sync', ...)` que consumiera la cola. **Solución (opción E)**: eliminados TODOS los repeat jobs de auto-sync (`sync.scheduler.ts` borrado, 5 call sites eliminados). El sync automático pasa a ser: (1) cron diario `03:00 UTC` — `background-sync.scheduler.ts` ahora arranca `new Worker('background-sync', ...)` que procesa los jobs `SyncBatchJobData`; (2) express-sync al abrir la app — `POST /sync/app-open` con cooldown server-side por tier (free 60 min / premium 15 min, `SET NX` en Redis `sync:appopen:{userId}`). Las TRES fuentes (cron, app-open, manual) convergen en jobId `sync-bg:{userId}` con `SyncBatchJobData` → deduplicación nativa BullMQ → nunca dos jobs del mismo usuario compitiendo por el lock. `triggerManualSync` también refactorizado para converger en `sync-bg:{userId}` (antes usaba `manual-sync:{userId}:{platform}` con ID auto-generado, sin dedup). A41 (cuota Steam) revivida: los repeat jobs la bypasaban; ahora todo pasa por `runBackgroundSyncs()` que sí la aplica. Tier leído fresco del JWT en cada app-open → sin estado congelado. Mobile: hook `useAutoSync` (cold start + AppState foreground) + `POST /sync/app-open`. Script `scripts/cleanup-auto-sync-jobs.ts` para limpiar los 6 repeatables `auto-sync:*` que quedaron de la versión vieja (ejecutar DESPUÉS del deploy — ver OP09). Tests: 673 API + 487 mobile, verde. **DESPLEGADO en producción (2026-06-29)**: A ✅ deploy limpio, B ✅ arranque sin `"Auto-syncs restaurados"`, C ✅ 6 keys `auto-sync:*` eliminadas de Redis (`getRepeatableJobs` = 0). Bug jobId `:`→`-` encontrado y resuelto durante el deploy (ver T112). Sync manual verificado: 375 juegos procesados sin errores. **Gate D — (a) ✅ VERIFICADO (2026-06-30)**: cron 03:00 UTC corrió a su hora; todos los usuarios estaban frescos (<24h sin sync) → cron sin trabajo, comportamiento correcto. 0 reencolados, 0 abandonos, 0 fallos (vs 12+3+9 hace 2 días). Ambos caminos de `runBackgroundSyncs` verificados: sin `userId` (cron) ✅ y con `userId` (manual) ✅. Lógica filtro `lte: oneDayAgo` confirmada correcta (selecciona dormidos). **Gate D — (b) ⏳ pendiente**: `useAutoSync` app-open verificable tras PL26 (build EAS). |
| T102 | ✅ **TTL de `sync:progress` reducido a 900 s** | Fix en commit `f97b302` (2026-06-28). Constante `SYNC_PROGRESS_TTL_SECONDS = 900` en `sync.worker.ts`. El TTL se renueva en cada callback `onBatch` (todos los adapters implementan `syncUserBatched`), así que actúa como "watchdog": si el sync muere sin más batches, la clave se autolimpia en 15 min en lugar de 2 h. 2 tests nuevos (TTL=900 en setex inicial, ausencia de TTL=7200). |
| T103 | ✅ **logger.info al inicio y fin de cada plataforma en `syncPlatform`** | Fix en commit `f97b302` (2026-06-28). `'[SyncWorker] Sync iniciado'` con `{userId, platform}` al arrancar; `'[SyncWorker] Sync completado'` con `{userId, platform, gamesUpdated, achievementsSynced}` al terminar con éxito. 3 tests nuevos (iniciado, completado con métricas, no-completado en error). |
| T104 | ✅ **`redis.del(syncProgressKey)` consolidado en bloque `finally` de `syncPlatform`** | Fix en commit `f97b302` (2026-06-28). Los dos `redis.del` dispersos (en catch del adapter y en path de éxito) se reemplazaron por un único `finally` que cubre todos los caminos: éxito, error del adapter, y errores en código post-adapter (prisma upsert/update). Los callers (batch y single-platform) mantienen su safety-net como segunda línea de defensa (doble del es harmless). 3 tests nuevos (error path, post-adapter error path, success path). |
| T105 | ✅ **`attempts:3` BullMQ — resuelto por dependencia con T102+T104** | El problema: cada reintento automático reescribía `sync:progress` reseteando el TTL. Con T104 (del en `finally`), cada intento limpia la clave al terminar (con o sin error). Con T102 (TTL=900), incluso si un intento muere sin limpiar, la clave expira en 15 min. Ninguna acción de código adicional requerida — cerrado por dependencia. |
| T106 | ✅ **PSN adapter sin timeout de aplicación** | `psn-api` no expone `AbortSignal` en sus parámetros públicos → implementado con `Promise.race` + `withPsnTimeout()` (helper privado en `psn.adapter.ts`). Las 9 llamadas a la librería quedan cubiertas: `exchangeNpssoForAccessCode`, `exchangeAccessCodeForAuthTokens`, `getProfileFromUserName`, `getUserTitles` (×2: `fetchUserTitles` + `checkPsnProfilePrivacy`), `getTitleTrophies`, `getUserTrophiesEarnedForTitle`, `getProfileFromAccountId` (legacy), `exchangeRefreshTokenForAuthTokens` (×2: `buildAuth` + `buildAuthWithRefresh`). Todas con `PSN_REQUEST_TIMEOUT_MS = 15_000`. Nota: los catch existentes (`PSN_PROFILE_PRIVATE`, `PSN_USER_NOT_FOUND`) envuelven el error de timeout con el código del catch — comportamiento conservador, no una regresión (antes también envolvían cualquier error). Test añadido (`T106: rechaza si getProfileFromUserName no responde en 15 s`) — usa `jest.useFakeTimers()` + `advanceTimersByTime(15_000)`. 663/663 API, 33/33 mobile. |
| T107 | ✅ **Race condition: paginación de Inicio vs sync concurrente (spinner del filtro colgado)** | Fix aplicado en `index.tsx` — `fetchAllRemainingPages` ahora incluye un guard de página vacía dentro del bucle: si la última página devuelta tiene `data.length === 0`, se hace `break` aunque `hasNextPage` sea `true`. Esto cubre el agravante server-side (inconsistencia de conteo entre `total` y los ítems reales paginables durante un sync concurrente). Fix mínimo: sin estado nuevo, sin refs adicionales, sin cambio en las dependencias del `useCallback`. Test añadido en `FeedScreen.test.tsx` — verifica que con `data:[]` + `hasNextPage:true`, `fetchNextPage` se llama exactamente 1 vez (sin el guard, el mock siempre devuelve `hasNextPage:true` y el bucle sería infinito). 33/33 tests mobile. El agravante primario (sync modifica `total` entre páginas → `loaded` nunca alcanza) queda mitigado indirectamente por el fix de T101 (batch sync con deduplicación BullMQ). Sort server-side diferido a Fase 4 (OP07+). |
| T108 | ✅ **Repeat jobs huérfanos de plataformas desvinculadas — RESUELTO (y causa estructural eliminada)** | **Parte B (código, commiteado 2026-06-29)**: `unlinkPlatform` y `deleteAccount` en `platform.service.ts` / `user.service.ts` cancelaban el repeat job de auto-sync con `cancelAutoSync` best-effort. **Parte A (operativo, 2026-06-29)**: 3 huérfanos preexistentes del usuario `cmpxvovbm4pov13pta55e5p3j` eliminados manualmente de Redis con `removeRepeatableByKey`. **Causa estructural eliminada (opción E, T101)**: con el refactor de auto-sync ya no existen repeat jobs de ningún tipo — `scheduleAutoSync`, `restoreAutoSyncs` y `cancelAutoSync` han sido eliminados del código. La Parte B queda obsoleta (no puede haber huérfanos de lo que ya no se crea). El flujo de borrado de cuenta/desvincular sigue siendo correcto — simplemente ya no tiene que cancelar ningún repeat job. Ver OP09 para limpiar los 6 repeatables `auto-sync:*` que aún quedan en Redis de la versión vieja. |
| T109 | ✅ **`scheduleAutoSync` genera reencolados que el batch no cubre — OBSOLETO (resuelto por T101 opción E)** | `scheduleAutoSync` eliminado por completo en el refactor opción E. Ya no existen repeat jobs ni llamadas a `scheduleAutoSync` — el mecanismo problemático fue erradicado, no parcheado. |
| T111 | ✅ **`.catch` silencioso en `cancelAutoSync` — OBSOLETO (resuelto por T101 opción E)** | `cancelAutoSync` eliminado junto con todos los repeat jobs en el refactor opción E. Ya no existe la función ni el patrón `.catch(() => undefined)` asociado. |
| T110 | 🔵 **Ruido de logs de Redis — persistencia RDB cada 60s** | Railway logs del servicio Redis muestran entradas periódicas de persistencia RDB (`BGSAVE`, `Background saving started`, `DB saved on disk`) cada ~60 s durante períodos de escritura activa (syncs). No es un bug: es el comportamiento esperado de Redis con `save 60 1` (política RDB por defecto — guardar si ≥1 clave cambió en los últimos 60s). No afecta al rendimiento ni a la persistencia de datos (AOF está activo para durabilidad). El ruido dificulta leer logs operativos relevantes (errores, warnings, sync completions) en Railway. **Opciones**: (a) subir el umbral RDB a `save 3600 1` — persiste 1×/hora en lugar de cada 60s, reduce el ruido sin comprometer la durabilidad (AOF cubre el intervalo); (b) desactivar RDB con `save ""` y confiar en AOF + snapshots manuales periódicos; (c) filtrar en Better Stack (Logtail) con exclusión de mensajes `BGSAVE`. Prioridad 🔵 — solo cosmético, sin impacto en datos ni disponibilidad. Detectado como ruido de fondo durante inspección de logs Railway (2026-06-28). |
| T112 | ✅ **Bug — BullMQ rechaza jobId con ':' (separador interno de claves Redis) — RESUELTO fix-forward (2026-06-29)** | BullMQ usa `:` como separador interno de claves Redis; cualquier jobId custom que lo contenga lanza `"Custom Id cannot contain :"` en el encolado. El refactor T101 usaba `sync-bg:{userId}` en 4 sitios runtime (`background-sync.scheduler.ts` + `sync.service.ts` ×2, contando `triggerManualSync` y `triggerAppOpenSync`) + 20 tests. El bug hacía que `triggerManualSync`, `triggerAppOpenSync` y el cron de las 03:00 UTC petaran al intentar encolar — ningún sync llegaba a procesarse. Causa de fondo: los tests mockean BullMQ y no validan el formato del jobId — **3.ª instancia del patrón "código muerto con tests verdes"** en este proyecto (batch sin consumer, y ahora jobId inválido). Fix forward: `sync-bg:{userId}` → `sync-bg-{userId}` (guion) en los 4 sitios runtime + 20 tests. Deduplicación intacta: mismo string en `name` y `jobId`. Verificado: ningún otro jobId con `:` en el codebase. Sync manual confirmado funcionando post-fix: 375 juegos procesados sin errores. 673 tests verde. **Verificado en ambos caminos (2026-06-30)**: manual (con `userId`) ✅ y cron (sin `userId`, 03:00 UTC) ✅ — ningún error `"Custom Id cannot contain :"` en logs. |
| T113 | 🟡 **Sync de biblioteca grande tarda ~50 min (escala lineal con nº de juegos)** | Observado con la biblioteca real del dev: PSN 375 juegos, ~11 % progresado en ~6 min → extrapolación ~50 min total. Riesgos: (a) ocupa un slot de `concurrency:5` del worker ~50 min; (b) varios usuarios con bibliotecas grandes a la vez → saturación de concurrencia + cron nocturno bloqueado durante horas; (c) UX: barra "sincronizando" activa ~50 min. El watchdog de 900 s (`SYNC_PROGRESS_TTL_SECONDS`, T102) sigue siendo seguro — mide el hueco entre batches, no la duración total del sync —, pero el margen se estrecha para bibliotecas muy grandes. La solución de fondo es T114 (sync selectivo). No bloquea el lanzamiento — prioridad 🟡 relevante al escalar en número de usuarios. |
| T114 | 🟡 **Sync selectivo / incremental (sesión de diseño dedicada antes de implementar)** | Siguiente paso natural tras T101: resolver "QUÉ se sincroniza en cada sync" (T101 resolvió "cómo y cuándo se disparan"). Idea central: no sincronizar la biblioteca entera cada vez — solo los juegos con actividad reciente o cuyo contador de logros cambió desde el último sync. **A diseñar**: criterio de "relevante" (actividad reciente / contador remoto distinto del local / últimos N jugados); sync incremental en app-open vs completo en cron nocturno; detección de "ya sincronizado" (`lastSyncedAt` por juego o comparación de contador); encaje con A51 (tope 100 Steam, que ya ordena por `rtime_last_played`). **Relación con T90**: complementario — T90 añade cursor de reanudación para recuperar juegos omitidos; T114 reduce el scope de lo que se sincroniza. **No arrancar en código sin sesión de diseño previa** — la interacción con el tope A51, el batch T101 y el cron es no trivial. |
| T115 | 🟡 **Bug: botón de reset de contraseña en el email no funciona** | El correo de "olvidé contraseña" se envía correctamente (`RESEND_API_KEY` ✅, acción B3 ✅), pero al pulsar el botón/enlace del email no ocurre nada visible. **A diagnosticar**: (a) ¿el enlace está mal construido? — verificar la URL exacta en el email recibido: debe ser `unlockhub://reset-password?token=…` (deep link) o una URL web que redirija al deep link; (b) ¿el deep link abre la app pero el flujo falla en `app/reset-password.tsx`? — revisar parsing del query param `token`; (c) confirmar que el flujo NO toca el hash existente — el token es un valor temporal aleatorio; la nueva contraseña se re-hashea al actualizar; nunca se descifra la contraseña vieja. Investigar con logs de Railway para el endpoint `POST /api/v1/auth/reset-password` durante un intento real. |
| T116 | 🔴 **Seguridad: API key Firebase Client sin restricción en repo público** | `apps/mobile/android/app/google-services.json` contiene la API key de Firebase Client commiteada en un repo público (GitHub Pages activo). **La API key de Firebase client NO es un secret como una contraseña** — va embebida en el APK y es conocida por el cliente. El riesgo real es una key SIN restricción: cualquiera puede invocar APIs de Google con esa key y generar consumo de cuota o factura. **Acción (en este orden, sin prisas)**: (1) Verificar las restricciones actuales en Google Cloud Console → Credentials → la key `android_client` — ¿está ya restringida a `com.unlockhubapp` + SHA-1 + solo las APIs que AdMob necesita?. (2) Si no está restringida, restringirla: Application Restrictions → Android apps, añadir package name `com.unlockhubapp` + SHA-1 del keystore de producción; API restrictions → Mobile Ads SDK + Firebase APIs necesarias. (3) Opcionalmente rotar después de restringir (más seguro, pero no urgente si la restricción queda correcta). **Restringir es más importante y urgente que rotar.** Un SHA-1 incorrecto o package name erróneo puede romper AdMob en el APK publicado — verificar con cuidado. Prioridad: esta semana. |
| T117 | 🟡 **Data Safety Play Store — preparar declaración completa antes de publicar activos** | La sección "Seguridad de los datos" de Play Console debe declarar todos los datos recopilados y compartidos. La app comparte datos con: **PostHog** (userId + eventos de analítica, servidores EU), **Sentry** (crashes sanitizados — sin email ni tokens gracias a `beforeSend`), **Google AdMob** (advertising ID, con UMP/consentimiento GDPR), **Cloudinary** (imágenes de perfil y banner), **Resend** (email en flujo de recuperación de contraseña), **RevenueCat** (historial de compras — gateado, añadir en Fase 4). Borrador de tabla de categorías: "Información personal" (email, nombre de usuario/bio/país), "Fotos y vídeos" (fotos de perfil y banner), "Actividad de la app" (logros, biblioteca, ranking), "Identificadores" (userId interno), "Diagnóstico" (crashes Sentry), "Financiero" (compras RevenueCat — añadir cuando se active). Al activar RevenueCat en Fase 4, actualizar la declaración añadiendo la categoría financiera. Declarar mal esta sección es motivo de rechazo de Play Store. |
| T118 | 🟢 **Privacy Policy: listar subprocesadores explícitamente** | La Privacy Policy publicada en https://jmreja27.github.io/UnlockHub/privacy-policy.html debe mencionar explícitamente los subprocesadores: PostHog (analítica, EU), Sentry (diagnóstico), Google AdMob (publicidad con consentimiento), Cloudinary (almacenamiento de imágenes), RevenueCat (compras, gateado hasta Fase 4), Resend (email transaccional). Revisar el documento actual y añadir sección de "Terceros / Subprocesadores" si no existe. Baja urgencia — no bloquea publicación pero es buena práctica GDPR. |
| T119 | 🟢 **Permiso RECEIVE_BOOT_COMPLETED — verificar uso real y eliminar si no se usa** | El permiso `RECEIVE_BOOT_COMPLETED` aparece declarado para la app. Verificar con `grep -rn "RECEIVE_BOOT_COMPLETED\|BOOT_COMPLETED" apps/mobile/` si algún plugin o código nativo lo utiliza actualmente. Si no se usa, eliminarlo del `app.json`: simplifica la declaración de permisos visible en Play Store, mejora la postura de mínimo privilegio y reduce la superficie declarada en Data Safety. Comprobar antes de tocar que ningún plugin como `expo-notifications` lo necesite para re-registrar tokens tras reinicio del dispositivo. |
| T120 | 🟢 **Push notifications desactivadas — documentar estado y plan de reactivación** | El hook `usePushNotifications` es actualmente un no-op (las push notifications reales requieren integración FCM/Firebase en Android). Documentar en CLAUDE.md o README por qué están desactivadas y cuáles son los pasos para reactivarlas: (1) configurar proyecto Firebase con credenciales correctas; (2) añadir `FCM_SERVER_KEY` a Railway Variables; (3) verificar que `expo-notifications` registra el `ExpoPushToken` correctamente en `POST /api/v1/notifications/device-token`; (4) re-smoke el flujo FRIEND_REQUEST + push en device real. Reactivar antes del próximo build de producción si se quieren incluir en el lanzamiento. |
| T121 | ✅ **Cosmético: mensaje de log BackgroundSync engañoso corregido** | `background-sync.scheduler.ts` línea 51: `'[BackgroundSync] Sin usuarios activos en las últimas 24h'` → `'[BackgroundSync] Sin cuentas con más de 24h sin sync — cron sin trabajo'`. El texto anterior sugería "no hubo usuarios activos" cuando la realidad es "no hubo usuarios INACTIVOS >24h" (el cron busca los dormidos, no los activos). Lógica del filtro (`lte: oneDayAgo`) confirmada correcta — solo el texto confundía. Sin cambios de lógica, sin cambios de tests (ningún test verificaba ese string). Lint + typecheck + suite verde (13/13). |

### 🔧 Operaciones y DevOps

| # | Tarea | Detalle |
|---|---|---|
| OP01 | Revisar crédito/plan de Railway | Al cierre de la sesión 2026-06-12 el saldo estaba en ~"2 días / $1.32". Verificar saldo actual en Railway dashboard → Billing y decidir si ampliar plan antes de que el servicio se interrumpa. |
| OP02 | Decidir política de autodeploy de develop | El autodeploy en develop causó dos incidentes el 2026-06-12 (INC-01): cada push a develop relanzaba `npx prisma migrate deploy` contra producción sin verificación previa. Opciones: (a) mantener autodeploy pero formalizar checklist de revisión de migraciones antes de cualquier push a develop; (b) desactivar autodeploy de develop y desplegar solo desde main en cada release. Documentar decisión en DECISIONS.md. |
| OP03 | Destrackear `apps/mobile/expo-env.d.ts` | Añadir `apps/mobile/expo-env.d.ts` a `.gitignore` + ejecutar `git rm --cached apps/mobile/expo-env.d.ts`. El archivo es generado automáticamente por `expo` y no debe estar versionado en el repo. |
| OP04 | Revisar necesidad del MCP de Railway tras el lanzamiento | El MCP de Railway está instalado en Claude Code (local stdio) de forma provisional (~2 meses, fase de lanzamiento). Evaluar tras el lanzamiento si se mantiene el acceso o se revoca. Ver política de permisos en CLAUDE.md sección "Railway MCP — política de permisos". |
| OP05 | Configurar `watchPaths` en `railway.json` (API y Worker) | Actualmente cualquier push a `develop`, aunque sea 100% mobile, reinicia ambos servicios de backend innecesariamente. Configurar `watchPaths` en `apps/api/railway.json` y `apps/worker/railway.json` para que Railway solo redespliegue cuando cambian `apps/api/`, `apps/worker/`, `packages/` o `prisma/`. Relacionado con OP02. Prioridad baja — post-lanzamiento. |
| OP06 | Renovar `PSN_SYSTEM_NPSSO` periódicamente (~60 días) | La cookie `npsso` caduca aproximadamente cada 60 días. Síntoma silencioso: `Sync fallido err="Expired token"` en logs de Railway — RA sigue funcionando con normalidad, solo PSN falla. El valor puede aparecer idéntico en el navegador aunque la sesión esté expirada — comparar strings no es diagnóstico fiable. Fix: logout + login en my.playstation.com → copiar nueva `npsso` desde DevTools (Application → Cookies) → actualizar en Railway dashboard → Variables. Programar recordatorio cada 50-55 días para no llegar a expiración en producción. Ver sección PSN de CLAUDE.md para diagnóstico completo. |
| OP07 | ⚠️ EAS Update (OTA) — **PRIORIDAD próximo build (versionCode 10)** | **Estado: AUSENTE en versionCode 9 (producción actual). Diagnóstico completo realizado 2026-06-27.** Las tres piezas que faltan (todas requieren nuevo AAB — no existe ruta OTA para activar OTA): `expo-updates` no instalado, bloque `updates` ausente en `app.json`, perfil `production` sin `channel` en `eas.json`. Pasos a ejecutar desde `apps/mobile/`: **(1)** `npx expo install expo-updates`; **(2)** añadir en `app.json` dentro de `"expo": { ... }`: `"updates": { "url": "https://u.expo.dev/13b5c86a-75d1-442e-92c3-26d76db4fb94", "fallbackToCacheTimeout": 0 }` y `"runtimeVersion": { "policy": "appVersion" }`; **(3)** añadir `"channel": "production"` al perfil `production` en `eas.json` — sin `channel`, el binario no sabe a qué rama de updates suscribirse y OTA queda inactivo aunque el paquete esté instalado; **(4)** `eas channel:create production` si el canal no existe; **(5)** nuevo EAS build (versionCode 10) + subir AAB a Play Store + **re-smoke completo (3/3) antes de promover** — el build con OP07 NO hereda el smoke de PL19. Una vez activo, JS/assets se publican con `eas update --channel production` sin nueva build ni revisión de Play Store. **Política `appVersion` elegida deliberadamente**: un OTA publicado para `runtimeVersion: "1.3.1"` solo llega a binarios con esa misma versión; al subir la versión en el próximo AAB los binarios viejos dejan de recibir updates (comportamiento esperado — OTA cura la versión actual, no las pasadas). La primera build con `expo-updates` sí requiere revisión de Play Store; las OTAs posteriores no. |
| OP08 | Verificar con sync de prueba inmediato tras cambiar CUALQUIER credencial de sistema | Siempre que se actualice `RA_SYSTEM_USER`, `RA_SYSTEM_KEY`, `PSN_SYSTEM_NPSSO` o `STEAM_API_KEY` en Railway Variables (o Shared Variables): **(1)** esperar redeploy automático; **(2)** lanzar un sync manual de un usuario que tenga esa plataforma vinculada; **(3)** confirmar en Railway logs que el job completa sin `Sync fallido` (y sin `httpStatus: 401` en el detalle — ver commit `f27d40c`). No asumir que el cambio es correcto solo porque Railway aceptó la variable. **Ligado a OP06** (renovar NPSSO): tras actualizar el `npsso`, ejecutar este procedimiento antes de dar el cambio por cerrado — el síntoma de NPSSO expirado puede tardar horas en aparecer si no hay syncs activos en curso. **Antecedente**: INC-RA-001 (2026-06-27) — `RA_SYSTEM_USER` y `RA_SYSTEM_KEY` con valores incorrectos tras migración a Shared Variables; detectado con ráfagas de `Sync fallido` en producción, no en el deploy. Ver T97 para la mejora de código complementaria (health check al arranque). |
| OP09 | ✅ **Deploy del refactor auto-sync (opción E, T101) — COMPLETADO (A/B/C ✅, gate D parcial)** | **Ejecutado 2026-06-29 en orden**: **(A)** ✅ deploy API + worker con código nuevo — worker arrancó sin errores. **(B)** ✅ arranque limpio confirmado — sin `"Auto-syncs restaurados"` en logs (presencia de ese mensaje habría indicado que el worker viejo seguía en producción). **(C)** ✅ `npx tsx scripts/cleanup-auto-sync-jobs.ts` ejecutado — 6 keys `auto-sync:*` eliminadas de Redis; `getRepeatableJobs()` total = 0 confirmado. **Bug T112 detectado y resuelto durante el deploy**: jobId `sync-bg:{userId}` rechazado por BullMQ (`:` es separador interno de claves Redis) — fix-forward `sync-bg-{userId}` en los 4 sitios runtime + 20 tests; 673 tests verde. **(D) Gate de verificación — estado**: (2) sync manual ✅ converge en `sync-bg-{userId}`, procesa 375 juegos sin errores; (3) cron 03:00 UTC — pendiente primera ejecución esta noche; (1) app-open — pendiente hasta PL26 (~2 días); (4)+(5) monitoreo continuo en Railway logs. |

### 🟢 Features

| # | Tarea | Estado |
|---|---|---|
| F1 | Estadísticas avanzadas (premium) | ✅ |
| F2 | Canje de puntos por premium | ✅ |
| F3 | Compartir logro | ✅ |
| F4 | Comparación de perfiles | ✅ |
| F5 | Push al desbloquear logro | ✅ |
| F6 | Retar a un amigo en logro | ✅ |
| F7 | Guías UGC de logros | ✅ |
| F8 | Avatar upload | ✅ Backend Cloudinary + mobile expo-image-picker — activo en prod (`CLOUDINARY_URL` configurada en Railway ✅) |
| F9 | Dashboard admin | ✅ |
| F10 | ✅ OG profiles | ✅ `GET /api/v1/users/:username/og` devuelve HTML con meta tags Open Graph (title, description, image, url). PRIVATE → 404. Share button en `profile/[username].tsx` comparte `https://unlockhub.app/u/{username}`. |
| F11 | Búsqueda de logros con filtro de plataforma | 🔲 Eliminado del Search tab en sesión 37 — hook `useSearchAchievements` y endpoint `GET /api/v1/search?type=achievements` intactos para uso futuro (T27) |
| F12 | SyncStatusBar — feedback de sync en biblioteca | ✅ Botón sync, syncs restantes (free), cooldown countdown, última sync, próximo auto sync |
| F13 | Activar premium + RevenueCat | 🟡 Alta — Fase 4. Código 100% intacto. Pasos: (1) completar B18/B19/B20 · (2) `FEATURES.premium = true` en `featureFlags.ts` · (3) `FEATURES.pointsRedeem = true` · (4) `FEATURES.advancedStats = true`. `react-native-purchases` v10, `usePremiumPlans`, `useSubscription`, `useRevenueCat`, `premium.tsx`, webhook backend — todo listo. ✅ Verificación de formato de fechas: `PremiumBanner.tsx` ya usa `formatFullDate` (sin Intl) para mostrar la fecha de expiración — corregido en auditoría 2026-06-27 antes de activar el flag. |
| F14 | PSN sync paralelo — `Promise.allSettled` con concurrencia 5 | ✅ `processSingleTitle()` extraído; `processTitles()` procesa chunks de 5 en paralelo con aislamiento de fallos por título |
| F15 | RA sync paralelo — `Promise.allSettled` con concurrencia 3 | ✅ `syncUser()` y `syncUserBatched()` procesan chunks de 3 juegos en paralelo con `Promise.allSettled` |
| F16 | SyncStatusBar — countdown local + aviso sync largo | ✅ Countdown `setTimeout`-chain independiente del `refetchInterval` 60s; aviso ámbar tras 30s de sync activo |
| F17 | Onboarding paso 4 — CTAs de vinculación de plataformas | ✅ Paso 4 con botones Steam/PSN/RA → `router.replace('/link-platform/[x]')`, CTA secundario "Hacer esto más tarde" |
| F18 | FriendshipButton consciente del estado de relación en perfil público | ✅ 5 estados (none/pending_sent/pending_received/accepted/blocked) · GET /api/v1/friends/status/:username · confirmación Alert en eliminar · sesión 35 |
| F19 | Banner upload (Cloudinary) | ✅ POST /api/v1/users/me/banner · Pressable 120px en profile.tsx · aspect 3:1 · crop/fill 1500×500 · sesión 42 |
| F20 | ✅ Ampliar placements de AdMob | ✅ Sesión 55. Banner Rankings (`unitId='rankings'`) + Banner Friends (`unitId='friends'`) integrados. `useWrappedInterstitial` (cooldown 24h AsyncStorage) en `wrapped/[year].tsx`. `useCompletedGamesInterstitial` (AsyncStorage por gameId) en `index.tsx`. `AdBanner` type ampliado a `'home'\|'search'\|'rankings'\|'friends'`. `.env.example` actualizado con los 6 IDs. ✅ Ad units `unlockhub_rankings_banner` + `unlockhub_friends_banner` creados en AdMob Console. ✅ EAS secrets `EXPO_PUBLIC_ADMOB_RANKINGS_BANNER_ID` + `EXPO_PUBLIC_ADMOB_FRIENDS_BANNER_ID` configurados en expo.dev. |
| F21 | ✅ Ver logros de otros usuarios + comparativa | ✅ Implementado sesión 61. `GET /:username/games` + `GET /:username/games/:gameId/achievements` con `authenticateOptional` y privacidad F29. `app/user-game/[username]/[gameId].tsx`: toggle "Sus logros"/"Comparar". Sección Biblioteca en `profile/[username].tsx`. `useUserGames` + `useUserGameAchievements`. Tests: 593 API + 364 mobile. |
| F22 | Activar retos semanales (`FEATURES.challenges = true`) | 🟠 Media — Fase 4. Backend `WeeklyChallenge` + `UserChallenge` + scheduler + workers implementados. Solo requiere cambiar el flag y crear retos iniciales en BD. ✅ Verificación de formato de fechas: `challenges.tsx` ya usa `formatFullDate` (sin Intl) para mostrar `endAt` — corregido en auditoría 2026-06-27 antes de activar el flag. |
| F23 | Activar canje de puntos (`FEATURES.pointsRedeem = true`) | 🟠 Media — Fase 4. Endpoint `POST /api/v1/subscriptions/redeem-points` implementado. Activar junto a `FEATURES.premium`. |
| F24 | Activar estadísticas avanzadas (`FEATURES.advancedStats = true`) | 🟠 Media — Fase 4. Pantalla y datos implementados. Activar junto a `FEATURES.premium`. |
| F25 | Xbox — vinculación y sync | 🔵 Cuando el volumen lo justifique — Fase 4. `xbox.adapter.ts` implementado y gateado. Requiere OAuth2 Microsoft Identity Platform + verificación de empresa + `XBOX_CLIENT_ID`/`XBOX_CLIENT_SECRET` en Railway. |
| F26 | App Store iOS | 🟢 Largo plazo — Fase 4. Apple Developer Program $99/año (V4). Requiere cuenta + certificados + TestFlight + listing App Store. |
| F27 | Torneos internos | 🟢 Largo plazo — Fase 4. Consultar abogado antes de implementar (Ley 13/2011 — juegos de azar España). Solo recompensas en puntos/días premium. |
| F28 | ✅ Mostrar versión de la app en perfil | ✅ `expo-constants` — texto "Versión {{version}} ({{build}})" al pie de la sección de ajustes en `profile.tsx`. i18n ES/EN. `testID="app-version"`. |
| F29 | ✅ Privacidad de perfil (3 niveles) | ✅ Implementado sesión 60. Prisma: `ProfileVisibility` enum + `User.profileVisibility @default(PUBLIC)` + migración. Backend: `getPublicProfile` (PRIVATE→404, FRIENDS_ONLY→403 si no es amigo), `compareProfiles` respeta visibilidad, `updateProfile` sincroniza Redis al cambiar, `upsertUserScore` omite zadd si no PUBLIC, `authenticateOptional` en `GET /users/:username`. Mobile: selector inline 3 opciones en ajustes de perfil, perfil público diferencia 403 vs 404. i18n ES/EN. 575/575 API · 355/355 mobile · 0 errores TS/lint. |
| F38 | Wrapped compartible como imagen | 🔲 Fase 4 / post-lanzamiento — pendiente de validar con analítica. FREE · motor de adquisición orgánica (el Wrapped se comparte hacia afuera → crece la red). Anual: gratis, completo y espectacular. Mensual: básico gratis, desglose profundo + histórico detrás de premium. **Primero a construir** post-lanzamiento antes que cualquier otro F4x. |
| F39 | Ligas privadas con amigos / temporadas | 🔲 Fase 4 / post-lanzamiento — pendiente de validar con analítica. Crear+unirse FREE; profundidad/histórico/ligas ilimitadas PREMIUM. Núcleo del pivot social. Construir solo tras validar que el social convierte (métricas: amigos activos / invitaciones aceptadas). |
| F40 | Comparativa profunda head-to-head con amigos | 🔲 Fase 4 / post-lanzamiento — PREMIUM · conversión principal. Mejora de la comparativa existente. El 🔒 que se mostrará bloqueado en Wrapped y en perfil de amigo como preview de qué ofrece premium. |
| F41 | Notificaciones de cercanía al platino y rareza | 🔲 Fase 4 / post-lanzamiento — básicas FREE (alerta al 90 % de logros de un juego), ricas/rivalidad PREMIUM (qué amigos están cerca del mismo platino). Retención entre Wrappeds. |
| F42 | Stats avanzadas individuales extendidas | 🔲 Fase 4 / post-lanzamiento — PREMIUM · soporte de conversión. Mejora de `advancedStats` ya contemplado: estadísticas históricas y comparativas vs la comunidad (percentil por plataforma, velocidad de unlock, rachas históricas). |
| F43 | Sync Steam más frecuente como ventaja premium | 🔲 Fase 4 / post-lanzamiento — PREMIUM. Reducir intervalo de sync automático Steam para usuarios premium (ej. cada 30 min vs 60 min free). Alinea coste-ingreso: los usuarios más activos con más juegos son los que pagan. Depende de A51/T90 para no exceder cuota diaria. |
| F44 | 🔲 Feature futura — renombrar nombre de usuario | Actualmente no es posible cambiar el `username` una vez registrado. Relevante para limpiar cuentas demo (ej. `TestUser99` visible en capturas de Play Store). Implementar en Fase 4: `PATCH /api/v1/users/me/username` con validación de unicidad, actualización en rankings Redis y caché pública `invalidateUserPublicCache`, cooldown de 30 días entre cambios. |
| F45 | 🔲 Estadísticas Wrapped extendidas — post-segunda-ronda-de-testing | Análisis de viabilidad completo realizado (2026-06-23). Retomar tras aprobación Google Play + gate de producción superado. **El backend ya calcula 10 stats — no rehacer**: `rarestAchievement`, `mostActivePlatform` y `mostProductiveDay` ya están implementados y mostrados en la UI. **Viables, cero queries, desplegables sin build móvil** (si solo se añaden al share text): (1) primer y último logro del año — ordenar `userAchievements` por `unlockedAt`, ya en memoria, devuelve `title`+`gameName`+`iconUrl`+`unlockedAt`; (2) mes más activo — agrupar por `unlockedAt.getUTCMonth()`, ya en memoria; (3) juegos únicos con actividad — `gameMap.size` ya calculado internamente en `computeStats` pero no devuelto. **Viables, requieren build móvil para renderizar UI**: (4) distribución mensual — array de 12 valores para mini-gráfico, cero queries extra; (5) distribución de rareza — buckets ultrarraro (<1%) / raro (1-5%) / poco común (5-15%) / común (>15%) usando `Achievement.rarity` ya en memoria — Steam/RA/PSN una vez aterrizado F46 — poblar `Achievement.rarity` para PSN desde `trophyEarnedRate` en el sync; (6) desglose XP por plataforma — suma `normalizedPoints` agrupada por `achievement.platform`, cero queries. **Viable con caveat**: (7) percentil global — `ZRANK`/`ZCARD` sobre sorted set Redis `ranking:global` (2 comandos, O(log n)), pero mide XP acumulado all-time, no por año; percentil anual exacto requeriría `ranking:year:<año>` sorted set a incrementar en `sync.worker.ts`. **Viable con prerrequisito (F46)**: platino más difícil — PSN SÍ expone `trophyEarnedRate` (% de jugadores) vía psn-api; comparar rareza cross-plataforma es viable una vez que `Achievement.rarity` esté poblado para PSN (ver F46). Mientras F46 no aterrice, limitar a Steam/RA. **Caveat franja horaria**: técnicamente derivable de `unlockedAt.getUTCHours()` ya en memoria, pero `unlockedAt` está en UTC y no se guarda timezone del usuario (`User.countryCode` existe pero no está en el select de `loadUserAchievements`); omitir hasta guardar timezone explícitamente. Todos los cálculos (1)-(6) van en `computeExtendedStats` — solo para Wrapped anual. |

| F46 | 🔲 Sistema de XP ponderado por rareza real — cobertura completa Steam/RA/PSN | **Premisa corregida (2026-06-25)**: PSN SÍ expone rareza real. `trophyEarnedRate` (string, ej. `"2.9"` = 2,9 % de jugadores) y `trophyRare` (bucket Sony: 0=Ultra Rara, 1=Muy Rara, 2=Rara, 3=Común) están disponibles vía psn-api en `getTitleTrophies()` y `getUserTrophiesEarnedForTitle()`, que el sync de PSN ya llama. El flujo de sync ya es por usuario, lo que satisface el requisito de la API v2 (PS5) de que `trophyEarnedRate` solo se devuelve en contexto de usuario que ha jugado el título — **verificar en el código de sync concreto al implementar**. **Objetivo**: reemplazar la fórmula de XP de PSN (proxy por tipo de trofeo: Bronce 15/Plata 30/Oro 90/Platino 300) por una curva basada en `trophyEarnedRate`, equivalente a la de Steam (≤5 % → 100 XP, ≤15 % → 50 XP, ≤30 % → 25 XP, >30 % → 10 XP), para que las 3 plataformas usen rareza real y el XP sea comparable. **Prerrequisito obligatorio — 2 pasos antes de tocar la fórmula**: (1) Cambiar el sync de PSN (`psn.adapter.ts`) para extraer `trophyEarnedRate` y escribirlo en `Achievement.rarity` al hacer upsert (actualmente `rarity: null` para todos los logros PSN). (2) Backfill de los logros PSN ya en BD: iterar `Achievement` con `platform = 'PSN'` y `rarity = null`, llamar a la API PSN con las credenciales del sistema para recuperar el `trophyEarnedRate` y actualizar. Estimar volumen antes del backfill (actualmente ~327 juegos PSN × media de trofeos). **Decisiones pendientes antes de implementar**: (a) Impacto retroactivo en XP y rankings — los logros PSN existentes recalcularán su `normalizedPoints`; decidir si se recalcula el XP histórico de todos los usuarios o solo se aplica la nueva fórmula a logros sincronizados a partir de F46; recalcular histórico mueve posiciones en el ranking global, lo que puede sorprender a usuarios actuales. (b) Scope — la propuesta afecta al XP de logros (`Achievement.normalizedPoints`), no a los puntos canjeables (`UserPoint`); confirmar que el scope no se amplía. (c) Curva %→XP — decidir si reutilizar la de Steam o definir una propia para PSN (trofeos PSN son más raros en promedio; el platino de 300 XP actual es un outlier que la curva de rareza podría mantener o eliminar). **Beneficio secundario**: desbloquea F45 — distribución de rareza en Wrapped y "platino más difícil del año" pasan a funcionar también para usuarios PSN. |

### 🔶 Post-lanzamiento — Verificaciones pendientes

| # | Tarea | Detalle |
|---|---|---|
| PL12 | ✅ Declaración Data Safety actualizada para PostHog | PostHog (N4) está activo desde el lanzamiento — se ha declarado en el formulario de Seguridad de los datos. Si se cambia el proveedor de analítica en Fase 4, actualizar la declaración para reflejar el cambio. |
| PL13 | ✅ Limpieza de usuarios de prueba antes de abrir a Producción pública | ✅ **Ejecutado en producción (sesión 59)**. 7 usuarios de prueba eliminados (SmokeTest3, test, testuser123, testuser456, Seithek, Joels, testuser1234). Preservados: `TestUser99` (revisión Google Play) y `Sovelyss` (cuenta desarrollador). 7.684 UserAchievements + 17 RefreshTokens + datos asociados eliminados. Catálogo intacto: 2.878 juegos + 134.928 logros. Redis rankings dejados para regeneración natural (URL interna no accesible desde local). Script soporta múltiples `--preserve-username`: `npx ts-node ../../scripts/cleanup-test-users.ts --preserve-username=TestUser99 --preserve-username=Sovelyss`. |
| PL14 | ✅ Edge-to-edge Android 15 validado en dispositivo físico | `edges={['left', 'right']}` en SafeAreaView de todos los tabs (sesión 54). Validado en dispositivo físico con Android 15 — contenido correctamente acotado por el header de React Navigation (top) y tab bar (bottom). Sin doble inset de status bar. |
| PL16 | ✅ Índices PostgreSQL de rendimiento en User | `createdAt`, `isPremium`, `lastSyncAt` — usados por admin.service y background-sync.scheduler. Migración `20260607000000_add_user_performance_indexes`. |
| PL17 | ✅ Caché Redis para endpoints F21 (`/users/:username/games` y `/users/:username/games/:gameId/achievements`) | TTL 5 min, invalidación en sync completion + cambio de profileVisibility. `invalidateUserPublicCache()` exportada. |
| PL18 | ✅ Bundle optimization: @expo/vector-icons imports directos | 11 archivos migrados de barrel `{ Ionicons } from '@expo/vector-icons'` a `Ionicons from '@expo/vector-icons/Ionicons'` — elimina glyph maps de FontAwesome (96 menciones), MaterialIcons, AntDesign, Feather, etc. Sentry @sentry-internal/replay+feedback: incluidos por @sentry/browser, no eliminables sin upgrade de SDK — revisar en Fase 4. |
| PL15 | ✅ Merge develop → main antes de promover a Producción | ✅ Completado sesión 59 — `git merge --no-ff develop` + `git tag v1.0.0` + push. main refleja exactamente el código de producción. |
| PL19 | ~~Supersedida por PL25~~ | Bugs de código de la primera ronda: todos cerrados (T61-T65, T69-T73, sesiones 70-72). Segunda ronda de fixes (BUG-01 a MEJ-02) completada en develop. **Ver PL25** para los pasos actuales de build + distribución + segunda solicitud. |
| PL20 | ✅ Smoke tests en device con build de release | **Resultados (2026-06-17)**: (1) UMP/CMP ✅ — formulario de consentimiento aparece antes de cualquier banner en device real; `consentResolved` fluye correctamente (A49 verificado). (2) AdMob banners ✅ — Home y Search cargan en preview; fix de `APPLICATION_ID` en manifest confirmado. (3) PostHog EU ✅ — eventos `app_open` e `identify` capturados en panel EU (proyecto 203333). (4) A51 Steam >100 juegos: cubierto por tests automáticos; verificación en device pendiente con cuenta real de >100 juegos — se confirmará en producción vía logs pino `{ userId, total, syncing, skipped }` (ver PL23). Escenario Steam quota 80 % (A41) no testeable en preview — sin herramienta para forzar umbral. |
| PL21 | ✅ Analítica lista para aprender | **Resultados (2026-06-17)**: PostHog EU activo (proyecto 203333). Eventos verificados en panel: `app_open` ✅, `identify` ✅. Eventos tipados conectados: `platform_linked`, `sync_completed`, `wrapped_shared`, `onboarding_completed`, `friend_challenged`. Nota: dashboard PostHog con métricas D1/D7 + `sync_completed` rate pendiente de configurar antes del día de lanzamiento. PostHog US original queda obsoleta — ver PL24.
| PL23 | ⚙️ Verificar A51 en producción con usuario de biblioteca grande | Cuando haya usuarios reales con >100 juegos Steam, verificar en Railway logs `{ userId, total, syncing, skipped }` que: (1) el primer sync procesa los 100 más recientes por `rtime_last_played`; (2) el segundo sync re-procesa los mismos 100 activos sin marcar los omitidos como vacíos; (3) los juegos omitidos recuperan progreso en syncs nocturnos (background-sync.scheduler). Sin usuario de muestra antes del lanzamiento, esto se valida en los primeros días de producción. |
| PL24 | ⚙️ Eliminar cuenta PostHog US obsoleta | La cuenta PostHog original era US (us.posthog.com). Se creó un nuevo proyecto EU (203333) como fix de región. Limpiar acceso a cuenta US para evitar confusión. Deuda menor — post-lanzamiento. |
| PL25 | ✅ Nueva EAS build (versionCode 9) + distribución a testers + segunda solicitud — **PROMOVIDO A PRODUCCIÓN (2026-06-27)** | Incluye BUG-01 (crash Intl), BUG-02 (navegación vinculación + PSN feedback), MEJ-01 (date picker nativo), BUG-03 (rewarded ad feedback de carga), MEJ-02 (layout comparación) y assets de marca finales (commit ec44b01). Smoke test 3/3 ✅ (sync tiempo real, `useSafeBack`, edge-to-edge Android 15). Segunda solicitud de producción aprobada por Google — versionCode 9 promovido a track de Producción. **Sin OTA** — `expo-updates` no incluido por falta de cuota EAS. Ver OP07 + PL26 para el próximo build (versionCode 10). Supersede PL19. |
| PL26 | ⚙️ Próximo EAS build — versionCode 10 con OTA (OP07) | Cuota EAS disponible ~4 días desde 2026-06-27. Pasos: **(1)** ejecutar los 5 pasos de OP07 (`expo-updates`, `app.json`, `eas.json channel`, `eas channel:create production`, `eas build --profile production`); **(2)** subir AAB versionCode 10 a Play Store (track prueba cerrada); **(3)** smoke test completo re-verificación (3/3) — **obligatorio aunque no haya cambios de funcionalidad**; (4) promover a Producción. Acumular cualquier fix adicional que surja antes de lanzar el build. |
| PL22 | ✅ Distribución — listing Play Store completado — pendiente outreach día lanzamiento | ✅ **Listing completado (2026-06-21)**: nombre "UnlockHub: Logros y Trofeos"; descripciones (breve y completa) orientadas a lo social — rankings, comparación con amigos, Wrapped, feed — sobre el tracking (que ya ofrecen Exophase/TrueAchievements). Icono y gráfico de funciones subidos. ✅ **5 capturas seleccionadas y ordenadas**: (1) comparación con amigo (Seithek, consentimiento obtenido), (2) Wrapped 2025, (3) rankings, (4) comparación de logros, (5) biblioteca. Descartadas: modal "Retar amigo" (renderizado incompleto) y perfil con banner de portada casero. ✅ **Solicitud de producción enviada** a Google (2026-06-21) tras 14 días de prueba cerrada con 12 testers. ⚙️ **Pendiente solo**: aprobación de Google + outreach el día D en subreddits r/Trophies, r/RetroAchievements, r/Steam (verificar reglas de autopromo de cada uno antes de publicar) y Discords de cazadores de logros. |

### 🎨 Sistema de cosméticos y economía de puntos

> Diseño aprobado en sesión 70. Implementar en Fase 3/4 cuando el sistema de puntos tenga utilidad real.

**Estado general**: 🔲 Pendiente de implementación

**Decisiones de diseño tomadas:**
- Duración de todos los cosméticos: 90 días
- Puntos por retos semanales: Fácil 100 pts · Medio 200 pts · Difícil 400 pts
- Apuestas: cooldown 1 vez/día por usuario concreto + máx 3 cada 3 días en total · máximo 50 pts apostables
- Hitos de racha: 7 días → 50 pts · 30 días → 200 pts · 100 días → 500 pts · 365 días → 1.500 pts
- Multiplicador de puntos: descartado

**Cosméticos y costes:**

Marcos de avatar (90 días):
- Bronce: 200 pts
- Plata: 500 pts
- Oro: 1.000 pts
- Platino: 2.000 pts

Colores de nombre (90 días):
- Comunes (azul, verde, amarillo): 200 pts
- Poco comunes (naranja, morado): 500 pts
- Raros (rojo, cyan): 800 pts
- Legendario (degradado dorado): 1.500 pts

Títulos (90 días) — lista provisional, revisar antes de implementar:
- Comunes (150 pts): "Cazador de Logros", "Empezando el Viaje", "Coleccionista", "Sin Vida Social", "Jugador Casual"
- Poco comunes (400 pts): "Completista", "Retro Gamer", "Trofeo Adicto", "Achievement Hunter", "Imparable", "Madrugador"
- Raros (800 pts): "Leyenda", "El Platino o Nada", "100% o Nada", "Maestro Retro", "No Tengo Vida", "Top 1", "Dios de los Logros"

**Features a implementar:**
| # | Feature | Estado |
|---|---|---|
| F30 | Marcos de avatar con duración 90 días (Bronce/Plata/Oro/Platino) | 🔲 |
| F31 | Títulos de perfil con duración 90 días — lista provisional, revisar antes de implementar | 🔲 |
| F32 | Color de nombre con duración 90 días (Común/Poco común/Raro/Legendario) | 🔲 |
| F33 | Boost de racha — hitos 7/30/100/365 días con bonus de puntos | 🔲 |
| F34 | Apuestas 1vs1 de puntos en retos — cooldown por usuario + global | 🔲 |
| F35 | Ranking de puntos separado del ranking de XP | 🔲 |
| F36 | Saldo de puntos visible en perfil (actualmente solo en premium.tsx gateado) | ✅ |
| F37 | Rewarded ad accesible desde perfil (actualmente solo en premium.tsx gateado) | ✅ |

---

### 📋 Decisiones de producto registradas

> No son tareas — son decisiones estratégicas que informan el roadmap. No implementar hasta validar con datos reales post-lanzamiento.

| Decisión | Detalle | Fecha |
|---|---|---|
| Pivot estratégico | Núcleo social/competitivo y stats, NO tracking como diferenciador. Paridad de tracking con Exophase/TrueAchievements/Trophies Hunter ya no es un diferenciador — es el precio de entrada. La diferenciación real está en la capa social (ligas, comparativas, rivalidad) y la gamificación visual (Wrapped, marcos, títulos). | 2026-06-13 |
| Regla de monetización | Gratis todo lo que crece la red o se comparte hacia afuera (Wrapped, perfil público, comparativa básica). Premium toda profundidad, histórico y capa de estatus (desglose mensual, ligas ilimitadas, comparativa head-to-head, stats históricas). Tier único a ~2-3 €/mes — sin múltiples tiers. | 2026-06-13 |
| Objetivo económico | ~60-100 €/mes (cubrir infraestructura + Claude Pro + pequeño ahorro), alcanzable con ~30-50 suscriptores. No es objetivo de "vivir de ello". | 2026-06-13 |
| Estrategia de lanzamiento | Lanzar con `FEATURES.premium = false` para maximizar instalaciones/reseñas y medir retención pura sin ruido de conversión. Reactivar premium semanas después cuando D7 y frecuencia de uso tengan baseline suficiente. | 2026-06-13 |
| Activación de premium | Métricas primarias de activación: retención D7 + frecuencia syncs/semana (pesan más que instalaciones totales). Definir umbral mínimo antes de activar `FEATURES.premium = true` + completar B18/B19/B20. Sin baseline, activar premium antes de tiempo solo genera reseñas negativas de usuarios que no han visto valor todavía. | 2026-06-13 |

---
