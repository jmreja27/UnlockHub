import { v2 as cloudinary } from 'cloudinary';

// Cloudinary lee CLOUDINARY_URL de process.env automáticamente.
// No se necesita configuración explícita si la variable está definida.
export { cloudinary };
