/**
 * Plazo fijo (en días) desde que un reto de logro es aceptado hasta que expira si nadie lo consigue.
 * Único sitio donde se define — ajustar aquí, no hardcodear en el servicio ni en el scheduler.
 */
export const ACHIEVEMENT_CHALLENGE_DURATION_DAYS = 7;
