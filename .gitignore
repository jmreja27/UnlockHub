# Guías privadas — no subir al repositorio
ACCOUNTS_GUIDE.md

# Dependencias
node_modules/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml

# Build
dist/
build/
.expo/
android/
ios/

# Variables de entorno — NUNCA subir ficheros con secrets reales
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development
.env.ops
.env.*
!.env.example
!.env.*.example

# Claude Code — configuración local del agente (no subir)
.claude/

# Documentación privada local — no subir al repositorio
PRODUCTION_ACCOUNTS.md
SCALING_GUIDE.md

# Credenciales de Firebase / Google Services — contienen claves de API reales
google-services.json
GoogleService-Info.plist

# Service account key de Google Play (usada por EAS Submit)
google-play-service-account.json
*.serviceaccount.json

# Certificados y claves privadas
*.pem
*.p7s

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Editores
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# Sistema operativo
.DS_Store
Thumbs.db

# Expo / React Native
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# TypeScript
*.tsbuildinfo

# Prisma
prisma/migrations/*.sql.bak
/app.json

# graphify — grafo de conocimiento generado, no versionar
graphify-out/
